# 测试案例与验收标准（草案）

- **文档编号**：TEST-QUOTE-001
- **版本**：0.1.0-draft

> **特别注意 1：不能只测试“模型回答像不像人”。** 必须测试字段来源、规则计算、人工审核拦截和任务恢复。
>
> **特别注意 2：测试数据必须脱敏。** 不使用真实客户名称、联系方式、未授权图纸或商业机密。

## 1. 测试层级

| 层级 | 范围 | 工具/方式 |
|---|---|---|
| 单元测试 | 规则、解析器、Schema | pytest |
| 集成测试 | Worker + Ollama + API mock | Docker/本地环境 |
| 端到端测试 | 网站→任务→本地→云端展示 | 腾讯云测试环境 |
| 回归测试 | 固定案例集重跑 | CI 或手动发布检查 |
| 人工评估 | 工程师评价结果 | 审核表 |

## 2. 脱敏测试案例集

```text
tests/fixtures/
  case_001_simple_part_pdf/
  case_002_sheet_metal_excel/
  case_003_inspection_fixture_images/
  case_004_pneumatic_press_fixture_mixed_files/
  case_005_missing_critical_information/
  case_006_urgent_high_risk_project/
  case_007_invalid_or_unreadable_file/
  case_008_api_retry_and_idempotency/
```

| 案例 | 预期重点 |
|---|---|
| 001 简单零件 | 正确分类、无需强制审核 |
| 002 钣金+Excel | 识别 BOM、材料、数量 |
| 003 检具图片 | OCR 低置信度时提出补充问题 |
| 004 气动压装 | 强制人工审核、识别压装力/接口缺失 |
| 005 资料缺失 | 不输出假装精确的固定报价 |
| 006 加急高风险 | 应用加急规则、人工审核 |
| 007 损坏文件 | 不崩溃、记录错误、继续处理其他文件 |
| 008 网络重试 | 不重复创建报价 |

## 3. 核心验收标准

### 3.1 功能验收

| 编号 | 验收项 | 通过标准 |
|---|---|---|
| F-01 | 任务领取 | Worker 可领取且不会重复处理已租约任务 |
| F-02 | PDF 解析 | 文本 PDF 可提取文本与页码 |
| F-03 | OCR | 图片文字有置信度标记 |
| F-04 | Excel | 可读取工作表、行数及 BOM 候选列 |
| F-05 | Schema | 100% 进入报价引擎的数据通过 Pydantic 校验 |
| F-06 | 价格来源 | 100% 最终价格含规则计算快照 |
| F-07 | 人工审核 | 满足任一强制条件时 100% 标记审核 |
| F-08 | 文件清理 | 成功和失败任务均在 24 小时内删除原始本地附件 |
| F-09 | 幂等性 | 同一任务重复 complete 不产生第二个 quote_id |
| F-10 | 权限 | 客户无法查看其他客户项目与内部数据 |

### 3.2 质量目标（MVP 初始门槛）

| 指标 | 初始目标 | 说明 |
|---|---:|---|
| JSON Schema 合法率 | ≥ 98% | 经一次修复后统计 |
| 项目大类分类一致率 | ≥ 85% | 与工程师标注对比 |
| 交付物提取准确率 | ≥ 85% | 以人工标注为准 |
| 关键缺失项召回率 | ≥ 90% | 载荷/接口/精度/交期/范围 |
| 高风险项目审核召回率 | 100% | 宁可多审，不可漏审 |
| 规则引擎计算确定性 | 100% | 相同输入得到相同结果 |
| 常规任务端到端时间 | ≤ 15 分钟 | 10 页 PDF + 1 个 Excel 基准 |
| 回传成功率 | ≥ 99% | 网络恢复后最终成功 |

> **特别注意**：报价金额与人工最终报价的偏差不建议在没有 30 个以上真实、脱敏、可比历史项目之前设为硬门槛。早期应优先控制“漏项、漏风险、漏审核”。

## 4. 报价规则测试

每条规则必须有至少一个正例和一个边界例。

| 规则 | 正例 | 边界例 |
|---|---|---|
| 完整度 < 0.80 审核 | 0.73 → 审核 | 0.80 → 按配置判断 |
| 报价 ≥ 50,000 审核 | 50,000 → 审核 | 49,900 → 不因金额审核 |
| 交期比 < 0.70 | 0.65 → 加急+审核 | 0.70 → 略紧/按配置 |
| 精度 ±0.05 mm | 应审核 | ±0.10 mm 不强制 |
| 零件数 > 80 | 强制审核 | 80 → 按 31–80 档 |

## 5. 发布前检查清单

```text
□ 所有单元测试通过
□ 脱敏案例集端到端跑通
□ 规则 YAML 校验通过
□ 所有强制审核规则有自动测试
□ Agent Token 未出现在日志或 Git
□ COS 桶不是公共读写
□ Ollama 未暴露公网端口
□ 回传 API 有幂等测试
□ 本地原始附件清理任务已启用
□ 至少两名业务人员复核报价说明模板
```

## 6. 模型/规则变更回归

更换模型、Prompt、OCR 引擎或报价规则前，必须：

1. 固定运行全部案例集；
2. 比较分类、关键字段、审核结果与报价快照；
3. 记录差异；
4. 对“高风险项目未被审核”设置零容忍；
5. 经负责人批准后发布新版本；
6. 保留可回滚版本。

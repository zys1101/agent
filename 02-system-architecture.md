# 系统架构文档（草案）

- **文档编号**：ARCH-001
- **版本**：0.1.0-draft
- **部署模式**：腾讯云网站 + 本地 AI 报价工作站

> **特别注意 1：本地模型不对公网开放。** Ollama 仅监听本机；云端不会访问本地 `11434` 端口。
>
> **特别注意 2：腾讯云 COS 必须使用私有桶。** 对象只通过短时签名 URL 在任务处理期间下载。
>
> **特别注意 3：报价系统是“AI 初步报价 + 人工审核”，不是无人值守正式报价系统。**

## 1. 逻辑架构

```text
客户浏览器
  │ HTTPS
  ▼
腾讯云网站 / API
  ├─ 客户表单、登录、项目页
  ├─ 内部审核后台
  ├─ 项目数据库（MySQL/PostgreSQL）
  ├─ 任务表（MVP）
  ├─ 腾讯云 COS 私有对象存储
  └─ 报价展示 / PDF / 通知
          ▲
          │ HTTPS 出站请求：领取、心跳、回传
          │
本地 Windows 工作站（RTX 4070 Super，12GB）
  ├─ Quote Agent Worker（Python）
  ├─ Ollama（本地 LLM）
  ├─ PDF / OCR / Excel 解析工具
  ├─ 报价规则引擎
  ├─ SQLite 审计与待回传缓存
  └─ 本地 RAG（第二阶段）
```

## 2. 组件职责

| 组件 | 部署位置 | 职责 |
|---|---|---|
| 前端网站 | 腾讯云 | 客户提交、文件上传、状态和报价展示 |
| 后端 API | 腾讯云 | 身份鉴权、项目管理、任务分配、结果持久化 |
| 数据库 | 腾讯云 | 项目、文件元数据、任务、报价、审核、审计 |
| COS 私有桶 | 腾讯云 | 客户附件及生成的报价文件 |
| Quote Worker | 本地电脑 | 领取任务、控制工作流、回传结果 |
| Ollama | 本地电脑 | 本地模型推理 |
| Parser/OCR | 本地电脑 | PDF、图片、Excel 信息提取 |
| Quote Engine | 本地电脑 | 规则化工时和报价计算 |
| 本地 SQLite | 本地电脑 | 断网恢复、审计快照、待回传结果 |
| 向量库 | 本地电脑，第二阶段 | 脱敏历史案例和内部知识检索 |

## 3. 数据流

1. 客户在网站提交项目并上传附件到 COS 私有桶。
2. 云端创建 `queued` 的报价任务。
3. 本地 Worker 每 15 秒调用一次 `POST /tasks/claim`。
4. 云端下发任务表单及 COS 短期签名下载 URL。
5. Worker 下载文件到本地临时目录，完成解析和报价。
6. Worker 回传结构化报价结果；云端保存报价版本。
7. 高风险/高金额项目进入内部审核；否则展示预算报价。
8. Worker 删除原始客户文件，保留必要的脱敏审计快照。

## 4. 网络与安全边界

| 路径 | 允许 | 禁止 |
|---|---|---|
| 浏览器 → 云端 | HTTPS 443 | 直接访问本地工作站 |
| 本地 Worker → 云端 | HTTPS 443 出站 | 明文 HTTP |
| 云端 → 本地 Worker | 不允许主动入站 | 访问 Ollama / 本地目录 |
| Worker → Ollama | `127.0.0.1:11434` 或 Docker host gateway | 对公网绑定 |
| Worker → COS | 仅短期签名 HTTPS URL | 公共读写桶 |

## 5. 可用性策略

- MVP 为单本地 Worker，电脑离线期间云端任务保持排队。
- 租约默认 30 分钟；Worker 每 60 秒心跳。
- 本地已完成但未回传的结果写入 SQLite，在网络恢复后重试。
- 云端对同一任务的完成请求使用幂等键，避免重复报价。
- 后续可新增第二台 Worker；云端按 capability 和租约分配任务。

## 6. 数据分级与保留

| 数据 | 推荐位置 | 默认保留 |
|---|---|---:|
| 客户原始附件 | 腾讯云 COS 私有桶 | 按合同/客户政策 |
| 本地临时附件 | `data/incoming` | 任务结束即删，最长24小时 |
| 解析摘要 | 云端数据库/本地审计 | 30天或按项目政策 |
| 报价快照 | 云端数据库 + 本地 | 365天 |
| Agent 日志 | 本地/云端日志 | 365天，脱敏 |
| 历史案例 RAG | 本地向量库 | 经审批后长期保留 |

## 7. MVP 不包含的架构能力

- CAD 原生格式解析；
- 多 GPU 推理集群；
- 高可用本地推理服务；
- 云端模型推理；
- 自动合同签署；
- 自动支付和 ERP 闭环；
- 自动发布无人工审核的高风险正式报价。
